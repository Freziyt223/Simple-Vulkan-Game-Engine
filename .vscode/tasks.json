{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Vulkan launch",
            "command": "${workspaceFolder}/start.bat",
            "group": {
                "kind": "test",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}